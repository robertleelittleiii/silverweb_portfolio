<%#=ajax_select2("category_id","artifact",@artifact, create_menu_lowest_child_list("",@artifact.department_id))%>  
<% @artifact.department_list.each do |department| %>
  <fieldset class="department">
    <legend><%=department%></legend>

    <%=create_group_checks_live("category_id", @artifact, create_menu_lowest_child_list(department,nil,false),"category", {:class=>"category-check"} )%>

  </fieldset>
<% end %>
<%#=create_group_checks_live("category_id", @artifact, create_menu_list("Top Menu"),"category" )%>

